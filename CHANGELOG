v0.12.6
- Fix issue for what audioLevelBar was making the app crash.
- Refactor of audioLevelBar.

v0.12.5
- Fix issue for what changing between projects, restore its watermark configuration.

v0.12.4
- In this version, we have included the hability to set project info (title, details and product type).

v0.12.3
- Disable 4k resolution due to app crashes until we find the problem

v0.12.2
- Fix a bug at export for what the app was crashing at setting the player up.

v0.12.1
- Fix a bug in edition for what the app was crashing by splitting a clip up.

v0.12.0
- Right now, by default, there is no transition time between clips anymore.
